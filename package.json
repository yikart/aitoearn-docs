{
  "name": "aitoearn-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "build:overrides": "tsc -p tsconfig.overrides.json",
    "check": "npm run build:overrides && mint validate && mint broken-links",
    "dev": "npm run build:overrides && mint dev",
    "sync:video-models": "node scripts/sync-video-generation-models.mjs"
  },
  "devDependencies": {
    "mint": "^4.2.659",
    "typescript": "^6.0.3"
  }
}
